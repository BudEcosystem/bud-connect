# Service/Clients
dapr >= 1.14.0
budmicroframe @ git+https://github.com/BudEcosystem/bud-microframe.git
# Pinned to the merge commit of BudModelCatalog-SDK PR #7 on main (2026-09-26). It builds on
# PR #6 (voice billing rules and the pricing-page scrapers) and publishes voice model ids the
# vendors accept instead of pricing labels: 11 non-models retired (Google STT pricing SKUs and
# instant-custom-voice, Speechmatics batch-*/melia-1/linden-1, Rev.ai whisper-large, Gladia
# async), 5 renamed (speechmatics real-time-* -> standard/enhanced, gladia real-time ->
# solaria-1, google_speech medical-* -> medical_*), and Google STT v2 chirp_3/chirp_2/telephony
# added. The first sync on this pin marks the 16 old uris inactive (under the 25 guard).
#
# Pinned because unpinned does NOT mean latest: Docker keys the `pip install -r` layer on this
# file, so an unpinned git URL freezes at whatever HEAD was current when that layer was first
# built. The nightly shipped a six-month-old catalog that way, and deepgram/elevenlabs seeded
# with zero models. Bump this SHA deliberately when the SDK moves, and only to a commit on main.
bud-model-catalog @ git+https://github.com/BudEcosystem/BudModelCatalog-SDK.git@d1422c9a874f411fe4e04e5e20f9e58ed3fd9543

# API
fastapi >= 0.111.1
uvicorn >= 0.30.3
pydantic >= 2.8.2
pydantic-settings >= 2.3.4

# Utils
structlog >= 24.4.0
cachetools >= 5.4.0

# Database
SQLAlchemy>=2.0.36,<2.1
sqlalchemy-utils>=0.41.2
psycopg
psycopg2-binary>=2.9.10
alembic>=1.13.3
alembic-postgresql-enum>=1.3.0

# License Extraction
openai>=1.0.0
beautifulsoup4>=4.9.0
PyPDF2>=3.0.0
json-repair>=0.1.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
bcrypt==4.1.3  # Pin to version compatible with passlib 1.7.4
python-multipart>=0.0.5
email-validator>=2.0.0

# Eval Module - OpenCompass API Integration
httpx==0.27.2
# Note: opencompass[api] package has compatibility issues with Python 3.11+ (pyext dependency)
# We use the OpenCompass REST APIs directly via httpx instead

# Eval Module - Dataset Sampler (requires opencompass utilities)
# Note: opencompass is installed separately in Dockerfile with --no-deps to avoid pyext dependency
mmengine-lite>=0.10.0
torch>=2.0.0
importlib-metadata>=6.0.0
prettytable>=3.0.0
tabulate>=0.9.0
typer>=0.19.0
numpy<2.0.0,>=1.23.4
pandas<2.0.0,>=1.5.0
datasets<4.0.0,>=2.12.0
tiktoken
