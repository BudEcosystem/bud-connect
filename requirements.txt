# Service/Clients
dapr >= 1.14.0
budmicroframe @ git+https://github.com/BudEcosystem/bud-microframe.git
bud-model-catalog @ git+https://github.com/BudEcosystem/BudModelCatalog-SDK.git

# API
fastapi >= 0.111.1
uvicorn >= 0.30.3
pydantic >= 2.8.2
pydantic-settings >= 2.3.4

# Utils
structlog >= 24.4.0
cachetools >= 5.4.0

# Database
SQLAlchemy>=2.0.36
sqlalchemy-utils>=0.41.2
psycopg
psycopg2-binary>=2.9.10
alembic>=1.13.3
alembic-postgresql-enum>=1.3.0

# License Extraction
openai>=1.0.0
beautifulsoup4>=4.9.0
PyPDF2>=3.0.0
json-repair>=0.1.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
bcrypt==4.1.3  # Pin to version compatible with passlib 1.7.4
python-multipart>=0.0.5
email-validator>=2.0.0

# Eval Module - OpenCompass API Integration
httpx==0.27.2
# Note: opencompass[api] package has compatibility issues with Python 3.11+ (pyext dependency)
# We use the OpenCompass REST APIs directly via httpx instead

# Eval Module - Dataset Sampler (requires opencompass utilities)
# Note: opencompass is installed separately in Dockerfile with --no-deps to avoid pyext dependency
mmengine-lite>=0.10.0
torch>=2.0.0
importlib-metadata>=6.0.0
prettytable>=3.0.0
tabulate>=0.9.0
typer>=0.19.0
numpy<2.0.0,>=1.23.4
pandas<2.0.0,>=1.5.0
datasets<4.0.0,>=2.12.0
tiktoken
